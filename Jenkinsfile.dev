pipeline {
    agent any
    triggers {
        pollSCM '* * * * *'
    }
    stages {
        stage('Process Resources') {
            steps {
                sh './gradlew makeFrontend --debug'
            }
        }
        stage('Run') {
            steps {
                sh './gradlew bootRun --debug'
            }
        }
    }
}
