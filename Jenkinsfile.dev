pipeline {
    agent any
    triggers {
        pollSCM '* * * * *'
    }
    stages {
        stage('Process Resources') {
            steps {
                sh './gradlew makeFrontend'
            }
        }
        stage('Run') {
            steps {
                sh './gradlew bootRun'
            }
        }
    }
}
